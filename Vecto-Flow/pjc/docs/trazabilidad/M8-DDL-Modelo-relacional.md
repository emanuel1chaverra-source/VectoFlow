## Tabla 8 — DDL → Modelo relacional → PK → FK → Restricciones

| Tabla DDL          | Modelo Relacional  | PK                                      | FK                                                                                                                                                              | Restricciones                                                                                                              |
|--------------------|--------------------|-----------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------|
| ROLES              | ROLES              | PKRol INT NOT NULL AUTO_INCREMENT       | —                                                                                                                                                               | NombreRol VARCHAR(50) NOT NULL; Descripcion VARCHAR(150) NULL                                                              |
| USUARIOS           | USUARIOS           | PKUsuario INT NOT NULL AUTO_INCREMENT   | FKRol INT NOT NULL → ROLES(PKRol) ON UPDATE CASCADE ON DELETE RESTRICT                                                                                          | Correo VARCHAR(100) NOT NULL UNIQUE; Contrasena VARCHAR(255) NOT NULL; Estado TINYINT(1) DEFAULT 1                         |
| EJERCICIOS         | EJERCICIOS         | PKEjercicio INT NOT NULL AUTO_INCREMENT | FKDocente INT NOT NULL → USUARIOS(PKUsuario) ON UPDATE CASCADE ON DELETE RESTRICT                                                                               | Titulo VARCHAR(100) NOT NULL; Estado TINYINT(1) DEFAULT 1                                                                  |
| VECTORES           | VECTORES           | PKVector INT NOT NULL AUTO_INCREMENT    | FKDimension INT NOT NULL → DIMENSION(PKDimension) ON UPDATE CASCADE ON DELETE CASCADE; FKUsuario INT NOT NULL → USUARIOS(PKUsuario) ON UPDATE CASCADE ON DELETE CASCADE | NombreVector VARCHAR(10) NOT NULL                                                                                    |
| DIMENSION          | DIMENSION          | PKDimension INT NOT NULL AUTO_INCREMENT | FKVectores INT NOT NULL → VECTORES(PKVector) ON UPDATE CASCADE ON DELETE CASCADE                                                                                | Tamaño INT NOT NULL CHECK(Tamaño > 0)                                                                                      |
| DETALLE_VECTOR     | DETALLE_VECTOR     | PKDetalle INT NOT NULL AUTO_INCREMENT   | FKVector INT NOT NULL → VECTORES(PKVector) ON UPDATE CASCADE ON DELETE CASCADE                                                                                  | Indice INT NOT NULL; Valor FLOAT NOT NULL; CONSTRAINT UQ_Vector_Indice UNIQUE(FKVector, Indice)                            |
| OPERACIONES_SUMA   | OPERACIONES_SUMA   | PKOperacion INT NOT NULL AUTO_INCREMENT | FKEstudiante INT NOT NULL → USUARIOS; FKVectorA INT NOT NULL → VECTORES; FKVectorB INT NOT NULL → VECTORES; FKEjercicio INT NULL → EJERCICIOS ON DELETE SET NULL | FechaOperacion DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP; Resultado TEXT NOT NULL                                        |
| METRICAS_DESEMPENO | METRICAS_DESEMPENO | PKMetrica INT NOT NULL AUTO_INCREMENT   | FKOperacion INT NOT NULL UNIQUE → OPERACIONES_SUMA ON DELETE CASCADE; FKEstudiante INT NOT NULL → USUARIOS ON DELETE RESTRICT                                   | Intentos INT NOT NULL DEFAULT 0; Aciertos INT NOT NULL DEFAULT 0 CHECK(Aciertos <= Intentos); TiempoSegundos INT NOT NULL DEFAULT 0 CHECK(TiempoSegundos >= 0) |